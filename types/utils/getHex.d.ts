/**
 * Return hexadecimal number with "0x" from decimal number
 * @param {number} [num=0] Decimal number
 * @return {number} Hexadecimal number
 */
export declare function getHex(num?: number): string;
